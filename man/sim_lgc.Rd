% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllFunctions.R
\name{sim_lgc}
\alias{sim_lgc}
\title{Simulate Count Time Series from Specified Model}
\usage{
sim_lgc(
  n,
  CountDist,
  MargParm,
  ARParm,
  MAParm,
  Regressor = NULL,
  Intercept = NULL,
  ntrials = NULL,
  ...
)
}
\arguments{
\item{n}{Integer. Length of the time series to simulate.}

\item{CountDist}{Character. Name of the marginal count distribution to simulate from.
Supported values include \code{"Poisson"}, \code{"Negative Binomial"}, \code{"ZIP"},
\code{"Generalized Poisson"}, \code{"Mixed Poisson"}, \code{"Binomial"}, etc.}

\item{MargParm}{Numeric vector. Parameters of the marginal distribution (e.g., mean, dispersion).}

\item{ARParm}{Numeric vector. Autoregressive (AR) parameters. Can be empty or \code{NULL} for no AR component.}

\item{MAParm}{Numeric vector. Moving average (MA) parameters. Can be empty or \code{NULL} for no MA component.}

\item{Regressor}{Optional matrix or data frame of covariates. Used for dynamic regression models.}

\item{Intercept}{Logical or numeric. Whether to include an intercept term. If numeric, should be 0 or 1.}

\item{ntrials}{Integer. Number of trials for the Binomial model (required if \code{CountDist == "Binomial"}).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A numeric vector of length \code{n}, containing the simulated count time series.
}
\description{
Simulates a univariate count time series of length \code{n} from a specified model
defined by a marginal distribution, ARMA dependence structure, and optional regression
covariates.
}
\details{
The simulation is based on a latent Gaussian copula framework, where the marginal distribution
is applied to transformed latent variables with dependence induced by an ARMA process.
}
\examples{
# Simulate Poisson-ARMA(1,1) series
sim <- sim_lgc(
  n = 100,
  CountDist = "Poisson",
  MargParm = 3,
  ARParm = 0.5,
  MAParm = -0.3
)
ts.plot(sim)

}
