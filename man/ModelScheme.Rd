% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllFunctions.R
\name{ModelScheme}
\alias{ModelScheme}
\title{Create and Validate Model Specification for Particle Filtering}
\usage{
ModelScheme(
  RegModel = NULL,
  df = NULL,
  EstMethod = "PFR",
  ARMAModel = c(0, 0),
  CountDist = "NULL",
  ParticleNumber = 5,
  epsilon = 0.5,
  initialParam = NULL,
  TrueParam = NULL,
  Task = "Evaluation",
  SampleSize = NULL,
  OptMethod = "L-BFGS-B",
  OutputType = "list",
  ParamScheme = 1,
  maxdiff = 10^(-8),
  ntrials = NULL,
  verbose = TRUE,
  nsim = NULL,
  ...
)
}
\arguments{
\item{RegModel}{An object of class \code{formula}, typically in the form \code{y ~ x1 + x2} used to specify
the regression model to be fitted, evaluated, generated, etc.}

\item{df}{Data frame with the series to be modeled and possible regressor variables.}

\item{EstMethod}{Character. Estimation method. Currently only \code{"PFR"} is supported.}

\item{ARMAModel}{Integer vector of length 2. Specifies the order of the AR and MA components.}

\item{CountDist}{Character. The name of the count distribution (e.g., "Poisson", "Negative Binomial", "ZIP").}

\item{ParticleNumber}{Integer. Number of particles to be used in the filter.}

\item{epsilon}{Numeric. Resampling occurs when the effective sample size (ESS) drops below \code{epsilon * N}.}

\item{initialParam}{Numeric vector. Initial parameter values for optimization.}

\item{TrueParam}{Numeric vector. True parameter values (used in simulation/synthesis, ignored otherwise).}

\item{Task}{Character. One of \code{"Evaluation"}, \code{"Optimization"}, \code{"Simulation"}, or \code{"Synthesis"}.}

\item{SampleSize}{Integer. Required when \code{Task == "Synthesis"}.}

\item{OptMethod}{Character. Optimization method, e.g., \code{"L-BFGS-B"}.}

\item{OutputType}{Character. Type of output, typically \code{"list"}.}

\item{ParamScheme}{Integer. Indicates the parameter configuration scheme.}

\item{maxdiff}{Numeric. Convergence criterion for optimization.}

\item{ntrials}{Integer. Number of trials (used in Binomial model).}

\item{verbose}{Logical. If \code{TRUE} (default), informative messages are printed during execution.}

\item{nsim}{Integer. Number of replications. Required when \code{Task == "Simulation"}.
Set to \code{FALSE} to suppress messages.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A named list containing model specifications, validated inputs, marginal distribution
functions (PDF, CDF, inverse CDF), initial and true parameters, parameter bounds,
parameter names, and utility values for likelihood evaluation.
}
\description{
Parses and validates the full model specification and specified options.
This function gathers model components (data, distribution,
dependence structure, optimization parameters, etc.) and returns a structured list used as
input to other core functions in the package.
}
\details{
This function validates the inputs (e.g., distribution support, ARMA structure, initial
parameter lengths), computes the number of model parameters, constructs marginal distribution
functions based on the count distribution, and configures parameter bounds. It is a required
pre-processing step before fitting or simulating count time series models.
}
